"""CLI subcommand families for the skill-driven workflow.

Each family (session, kg, draft, validate, bundle) is a Typer sub-app that
plugs into the top-level ``wikify`` CLI in ``src/wikify/cli.py``.
"""
