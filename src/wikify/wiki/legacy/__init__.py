"""Legacy sitemap-first wiki flow.

Kept under ``wiki/legacy`` to make clear that the active wiki runtime
is the epoch / query / maintain / campaign path. The sitemap-first
modules here are retained because the CLI still exposes them and a
handful of tests cover them. They are slated for deletion once the
remaining CLI commands are migrated to the agent-native runtime.
"""
